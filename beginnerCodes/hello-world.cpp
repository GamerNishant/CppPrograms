#include<bits/stdc++.h>

using namespace std;

//Functions code

int main()
{

     #ifndef ONLINE_JUDGE 
    freopen("input.txt", "r", stdin) ;
    freopen("output.txt", "w", stdout) ;
    #endif
    ios_base::sync_with_stdio(false);
    cin.tie(NULL) ; cout.tie(NULL) ;

    cout<<"This is the program for making life easier to run and make things good.";

    return 0;

}