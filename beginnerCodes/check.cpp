#include <bits/stdc++.h>

using namespace std;

//Functions code

void check(int a)
{
    if (true)
    {
        cout << "Nishant Singh" << endl;
    }
    else
    {
        cout << "Happy Henry likes." << endl;
    }

    return;
}

int main()
{

#ifndef ONLINE_JUDGE
    freopen("input.txt", "r", stdin);
    freopen("output.txt", "w", stdout);
#endif
    ios_base::sync_with_stdio(false);
    cin.tie(NULL);
    cout.tie(NULL);
    cout.setf(std::ios::boolalpha);

    cout << "Nishant Singh" << endl;
    // Let's say how you format this.abort
    //Make right choice
    // I don't know why these things are not working and getting messy here cause I installed may
    // be too much in my vscode.
    return 0;
}
